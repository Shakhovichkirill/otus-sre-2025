{{- if .Values.goApp.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-app.goApp.name" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "sre-app.goApp.labels" . | nindent 4 }}
  {{- with .Values.goApp.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.goApp.service.type }}
  ports:
    - port: {{ .Values.goApp.service.port }}
      targetPort: {{ .Values.goApp.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "sre-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: go-app
{{- end }}